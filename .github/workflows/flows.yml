name: CosyVoice
on:
  workflow_dispatch:


jobs:
  build:
    uses: carv-protocol/ci/.github/workflows/build.yml@main
    secrets: inherit
  deploy:
    needs: build
    uses: carv-protocol/ci/.github/workflows/deploy.yml@main
    secrets: inherit
    with:
      version: ${{ needs.build.outputs.version }}
      cluster: ai
